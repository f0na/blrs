# Vercel 的构建镜像自带 rustc 1.92.0, 比 sqlx 0.9 要求的 1.94.0 还旧, 构建直接失败。
# 镜像里那个版本没有任何配置项能改 (@vercel/rust 只检查 rustup 在不在, 在就用镜像自带的),
# 所以只能在这里钉一个版本 —— rustup 会自己把这个 toolchain 装进构建环境。
#
# 本地也跟着一起钉住, 保证两边编译器完全一致。升级本地 rustc 时记得同步改这里。
[toolchain]
channel = "1.98.1"
